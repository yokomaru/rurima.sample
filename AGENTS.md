## プロジェクト概要

### サービス名
- るりまガチャ

### 目的
- Rubyのメソッドと偶然出会うための小さなWebサービス
- Rubyには便利なメソッドがたくさんあるが、なかなか知らないメソッドに触れる機会は少ない
- ガチャ形式でランダムにメソッドを紹介することで「見たことがある」メソッドを増やし、Rubyのメソッドについて脳内に索引を作るきっかけを提供する
- 楽しく気軽に何度も試したくなるUIを目指す

## 技術構成
### データ生成
- Ruby
- Bundler
- BitClust

#### データは以下の経路で生成する
- rurema/doctree
  - BitClust DB
    - methods.json

### フロントエンド
- HTML
- CSS
- Vanilla JavaScript
- ES Modules

### データ
- 静的な`methods.json`を配置する

### ブラウザ保存
- `localStorage`を使用する
- 直近3回のガチャ履歴のみ保存する

### 配信
- GitHub Pages
- mainブランチの `/docs` を公開する

## データ仕様
- MVPでは以下を対象とする
  - Ruby 4.0
  - BitClust DBの `_builtin`
  - public methodのみ
  - 使用例がないメソッドも対象に含める
  - 使用例が存在しない場合は `example: null`

- 公開するのは生成済みの `methods.json` のみとし、BitClust DB自体はWebアプリから配信しない

## 開発方針
- MVP完成を優先する
- タスクを小さく分けて1つずつ実装する
- 指定されたタスク以外へ勝手に進まない
- 要件にない機能を勝手に追加しない
- 不明な仕様がある場合は、推測で決めず実装前に確認する
- 不要なライブラリやフレームワークを追加しない
- 既存仕様を変更する場合は理由を説明する
- エラーが発生した場合は、原因を確認してから修正する
- 変更内容に応じて必要な動作確認を行う、またはこちらに依頼する
- 外部ライブラリやOSSを利用する際は、実装前に公式ドキュメント・公式リポジトリを確認する
- 公式に推奨された手順がある場合は、独自実装より優先する
- 指定されたタスク以外のファイルを勝手に追加・変更しない
- READMEやドキュメントは、明示的に依頼された場合のみ作成・更新する
- 外部リポジトリのcloneや生成DBなどの一時データは、Git管理下に作成しない
- 公式の中で今回の用途に推奨されている入口はどれか？まで確認する
- 外部データを公開・再配布する場合は、公開前に公式のライセンス条件を確認し、必要な出典・ライセンス・加工内容の表記を行う